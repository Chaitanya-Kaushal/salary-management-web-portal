# Loaded by `next dev` automatically. Override locally with `.env.local`.

# Point at a local backend (run salary-management-api-server on :4000)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Use the in-browser MSW mocks during dev so the UI works without a real backend
NEXT_PUBLIC_API_MOCKING=true
