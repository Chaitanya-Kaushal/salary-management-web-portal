NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: turn on browser-side MSW in dev so the frontend works without a real backend.
# Set to "true" to enable. Leave unset (or "false") for normal real-API behaviour.
# NEXT_PUBLIC_API_MOCKING=true
